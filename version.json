{"version":"2.14.22","note":"Data safety: a single table collapsing to empty on one device (e.g. after a failed load) can no longer delete that whole table from the server. The delete guard now also checks each entity on its own, not just the device-wide total — so a bad load of one module keeps the server copy safe.","released":"2026-09-19 17:13"}