{
  "name": "shic-pm-suite",
  "version": "2.1.1",
  "description": "SHIC Enterprise PM Suite",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "test:setup": "node tests/global-setup.js",
    "test:unit": "node --test tests/unit/**/*.test.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "dotenv": "^16.4.5"
  }
}
